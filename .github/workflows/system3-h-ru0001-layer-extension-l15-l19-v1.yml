name: SUPERSEDED - System3 H Ru0001 bounded L15-L19 layer extension v1

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  superseded:
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Refuse superseded computation path
        shell: bash
        run: |
          echo 'SUPERSEDED_WORKFLOW: no computation is authorized from v1.'
          echo 'The valid L15/L17/L19 cases are preserved in run 35060420908.'
          echo 'Adjudication was mechanically recovered without recomputation in run 35080334319.'
          exit 1
